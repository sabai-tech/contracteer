openapi: 3.0.0
info:
  title: Order API
  version: '1.0'
servers:
  - url: 'http://localhost:3000'
paths:
  /products:
    get:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                prop1:
                  type: array
                  items:
                    type: string
                  enum:
                  - ["cat", "dog"]
                  - ["john", "jane"]

      responses:
        200:
          description: OK