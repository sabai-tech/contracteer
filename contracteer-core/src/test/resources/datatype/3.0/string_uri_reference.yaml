openapi: "3.0.3"
info:
  title: Order API
  version: '1.0'
servers:
  - url: 'http://localhost:3000'
paths:
  /products:
    get:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                prop1:
                  type: string
                  format: uri-reference
                  enum: ["/api/v1", "/api/v2"]
      responses:
        200:
          description: OK