openapi: "3.1.0"
info:
  title: Test API
  version: '1.0'
paths:
  /probe:
    get:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                prop1:
                  $ref: '#/components/schemas/A'
                  maxLength: 3
      responses:
        200:
          description: ok
components:
  schemas:
    A:
      $ref: '#/components/schemas/B'
      maxLength: 5
    B:
      $ref: '#/components/schemas/A'
      maxLength: 10