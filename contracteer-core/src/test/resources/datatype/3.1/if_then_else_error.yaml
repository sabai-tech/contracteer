openapi: "3.1.0"
info:
  title: Test API
  version: '1.0'
paths:
  /products:
    get:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                prop1:
                  if:
                    type: string
                  then:
                    minLength: 1
                  else:
                    type: integer
      responses:
        200:
          description: ok
