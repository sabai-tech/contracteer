openapi: "3.1.0"
info:
  title: Test API
  version: '1.0'
paths:
  /products:
    get:
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                prop1:
                  $ref: '#/components/schemas/BigObj'
                  minProperties: 2
      responses:
        200:
          description: ok
components:
  schemas:
    BigObj:
      type: object
      minProperties: 1
      properties:
        a:
          type: string
        b:
          type: string
        c:
          type: string