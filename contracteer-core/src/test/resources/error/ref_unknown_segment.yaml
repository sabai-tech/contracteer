openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
paths:
  /x:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Person/foo'
      responses:
        '200':
          description: ok
components:
  schemas:
    Person:
      type: object
      properties:
        name:
          type: string