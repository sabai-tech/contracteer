openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
paths:
  /x:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                foo:
                  $ref: '#/components/parameters/queryFilter'
      responses:
        '200':
          description: ok
components:
  parameters:
    queryFilter:
      in: query
      name: filter
      schema:
        type: string