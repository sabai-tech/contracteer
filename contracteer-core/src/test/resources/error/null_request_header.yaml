openapi: "3.1.0"
info:
  title: Standalone Null Request Header
  version: '1.0'
paths:
  /things:
    get:
      parameters:
        - name: X-Tombstone
          in: header
          schema:
            type: ["null"]
      responses:
        '200':
          description: OK
