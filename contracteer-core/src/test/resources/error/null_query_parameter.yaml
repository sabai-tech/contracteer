openapi: "3.1.0"
info:
  title: Standalone Null Query Parameter
  version: '1.0'
paths:
  /things:
    get:
      parameters:
        - name: tombstone
          in: query
          schema:
            type: ["null"]
      responses:
        '200':
          description: OK
