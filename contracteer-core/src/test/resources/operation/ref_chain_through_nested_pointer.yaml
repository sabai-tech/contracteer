openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
paths:
  /x:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                nested:
                  $ref: '#/components/schemas/Container/properties/inner'
      responses:
        '200':
          description: ok
components:
  schemas:
    Container:
      type: object
      properties:
        inner:
          $ref: '#/components/schemas/Inner'
    Inner:
      type: integer