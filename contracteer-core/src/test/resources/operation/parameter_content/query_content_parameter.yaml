openapi: 3.0.0
info:
  title: Query Parameter Content Tests
  version: '1.0'
paths:
  /items:
    get:
      parameters:
        - in: query
          name: filter
          required: false
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                  limit:
                    type: integer
      responses:
        '200':
          description: Success