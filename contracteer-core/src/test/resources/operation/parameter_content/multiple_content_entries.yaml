openapi: 3.0.0
info:
  title: Multiple Content Entries Test
  version: '1.0'
paths:
  /items:
    get:
      parameters:
        - in: query
          name: filter
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
            text/plain:
              schema:
                type: string
      responses:
        '200':
          description: Success
