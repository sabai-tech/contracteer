openapi: 3.0.0
info:
  title: PipeDelimited Object Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: query
          name: filter
          style: pipeDelimited
          schema:
            type: object
            properties:
              color:
                type: string
      responses:
        '200':
          description: Success
