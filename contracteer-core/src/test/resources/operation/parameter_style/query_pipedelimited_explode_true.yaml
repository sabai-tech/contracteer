openapi: 3.0.0
info:
  title: PipeDelimited Explode True Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: query
          name: status
          style: pipeDelimited
          explode: true
          schema:
            type: array
            items:
              type: string
      responses:
        '200':
          description: Success
