openapi: 3.0.0
info:
  title: Header Simple Array Property Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: header
          name: X-Filter
          schema:
            type: object
            properties:
              tags:
                type: array
                items:
                  type: string
      responses:
        '200':
          description: Success
