openapi: 3.0.0
info:
  title: Cookie Form Nested Object Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: cookie
          name: session
          schema:
            type: object
            properties:
              address:
                type: object
                properties:
                  city:
                    type: string
      responses:
        '200':
          description: Success
