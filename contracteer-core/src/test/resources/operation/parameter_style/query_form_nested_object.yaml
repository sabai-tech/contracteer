openapi: 3.0.0
info:
  title: Query Form Nested Object Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: query
          name: filter
          schema:
            type: object
            properties:
              address:
                type: object
                properties:
                  city:
                    type: string
      responses:
        '200':
          description: Success
