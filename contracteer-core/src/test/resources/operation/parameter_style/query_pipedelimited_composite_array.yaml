openapi: 3.0.0
info:
  title: PipeDelimited Composite Array Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: query
          name: status
          style: pipeDelimited
          explode: false
          schema:
            oneOf:
              - type: array
                items:
                  type: string
              - type: array
                items:
                  type: integer
      responses:
        '200':
          description: Success