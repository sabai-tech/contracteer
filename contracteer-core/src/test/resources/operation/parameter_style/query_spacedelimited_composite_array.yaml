openapi: 3.0.0
info:
  title: SpaceDelimited Composite Array Test
  version: '1.0'
paths:
  /test:
    get:
      parameters:
        - in: query
          name: status
          style: spaceDelimited
          explode: false
          schema:
            oneOf:
              - type: array
                items:
                  type: string
              - type: array
                items:
                  type: integer
      responses:
        '200':
          description: Success