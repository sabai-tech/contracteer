openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
paths:
  /accounts:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                accountId:
                  $ref: '#/components/parameters/bankAccountId/schema'
      responses:
        '200':
          description: ok
components:
  parameters:
    bankAccountId:
      in: query
      name: bankAccountId
      schema:
        type: integer