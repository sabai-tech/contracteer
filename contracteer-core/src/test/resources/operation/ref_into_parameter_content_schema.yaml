openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
paths:
  /x:
    post:
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                filter:
                  $ref: '#/components/parameters/filter/content/application~1json/schema'
      responses:
        '200':
          description: ok
components:
  parameters:
    filter:
      in: query
      name: filter
      content:
        application/json:
          schema:
            type: integer