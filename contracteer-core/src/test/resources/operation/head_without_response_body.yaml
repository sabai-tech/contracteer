openapi: 3.0.0
info:
  title: Test API
  version: '1.0'
servers:
  - url: 'http://localhost:3000'
paths:
  /resources/{id}:
    parameters:
      - in: path
        name: id
        required: true
        schema:
          type: integer
    head:
      responses:
        200:
          description: Resource exists
          headers:
            x-request-id:
              schema:
                type: string
